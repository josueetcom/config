# Confirmation on remove, move, and copy
alias rm="rm -i"
alias mv="mv -i"
alias cp="cp -i"

# Git aliases (some of these override any commands with the same name)
alias ga="git add"
alias gb="git branch"
alias gc="git commit -m"
alias gca="git commit -am"
alias gd="git diff --color=always"
alias gg="git checkout"
alias gm="git merge"
alias gr="git remote"
alias gp="git push"
alias gs="git status"
alias gu="git pull"

# Faster-ish navigation
alias ..="cd .."
alias ...="cd ../.."

# Startup local server
alias serv="python -m SimpleHTTPServer"
